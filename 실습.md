
**이 파일은 마크다운(.md)파일입니다
visual studio code로 열고 markdown preview로 보시면 제일 편함!**

---
### git 설치

https://codingapple.com/unit/git-install-windows-mac/

하고 오세용.

### visual studio code 설치

https://code.visualstudio.com/

하고 오세용

### github 회원가입

https://github.com/

참고로, 뭔가 잘 안되면 브라우저 시크릿 모드에서 해보세용

### github cli (gh cli) 설치

https://cli.github.com/

여기 들어가면 위에 나오는 명령어 COPY해서 설치하면 대용
윈도우라면 powershell
맥이라면 terminal을 사용해보세용

---

## 기본 세팅

일단 컴퓨터 전원을 껐다가 다시 켜봅니다 
(뭔가 설치하고 나서 적용을 시키기 위해서)
### 기본 에디터 설정


```shell
# =======================================
# #으로 시작하는 줄은 주석입니다. 
# 주석은 빼고 코드만 복붙하세요
# =======================================

# 기본 에디터를 visual studio code로 지정하는 명령어
git config --global core.editor "code --wait"

```


### gh cli 이용한 로그인
```shell
# =======================================
# git 인증을 gh cli로 하기
# =======================================

# 웹사이트 열리면 로그인하기
# 그다음 터미널에 뜨는 코드를 웹사이트가 열리면 복붙하면 됩니다
gh auth login -p https -w -c

# git의 기본 인증을 gh cli로 지정
gh auth setup-git

# 제대로 됐는지 상태 확인
gh auth status
```


### git 설정 설정

```shell
# 아래를 터미널에 입력하면 visual studio code가 떠야함
# 만약 안뜬다면 "기본 에디터 설정" 절로 다시 가세요
git config --global --edit



```

### visual studio code로 설정파일 수정하기
### credential 부분은 건드리지 마세요! (예시와 달라도 상관없)
```shell
[credential "https://github.com"]
    helper =
    helper = !/usr/bin/gh auth git-credential

[credential "https://gist.github.com"]
    helper =
    helper = !/usr/bin/gh auth git-credential
```
### 나머지 부분은 수정 혹은 추가 하세용
```shell
[user]  
	name = 이름되도록영어로
	email = 이메일 
[init]  
	defaultBranch = main  
[core]  
	editor = code --wait  
[pull]  
	rebase = false  
[push]  
	autoSetupRemote = true
```


## 레포지토리 세팅

### github에서 레포지토리 만들기

1. gihub.com 접속
2. 깃허브 로그인 (당연히)
3. 프로필에서 레포지토리 들어가기
4. NEW 버튼 누르기 
5. 이름과 설명만 적고 다른건 건드리지 않기!!!!
6. 생성 버튼 누르기


### init의 경우 

1. 레포지토리에 보면 복붙하라고 아예 예시코드를 줌
```shell
# README 생성
echo "# repository-name" >> README.md

# 현재 디렉토리를 Git 저장소로 초기화
git init

# README를 staging area에 추가
git add README.md

# 첫 커밋 생성
git commit -m "first commit"

# 현재 브랜치 이름을 main으로 변경
git branch -M main

# GitHub 원격 저장소 연결
git remote add origin https://github.com/USERNAME/repository-name.git

# main 브랜치를 GitHub에 최초 push
git push -u origin main
```


## 개발하기

앞으로 개발할 때는 아래 과정을 계속 반복하면 대용.

```text
main 에서 최신화 (pull)
    ↓
브랜치 생성
    ↓
코드 수정 / 개발
    ↓
add
    ↓
commit
    ↓
push
    ↓
Pull Request(PR)
    ↓
Merge
    ↓
main으로 돌아오기
    ↓
pull
```

### 시작하기 전에 main 최신화

새로운 작업을 시작하기 전에 일단 `main` 브랜치로 가세용.

```shell
# main 브랜치로 이동
git switch main

# github에 있는 최신 내용을 가져오기
git pull
```

처음 만든 레포지토리라서 혼자 작업하고 있다면 별 차이가 없을 수도 있어용.

하지만 협업할 때는 다른 사람이 `main`에 새로운 코드를 추가했을 수도 있으므로  
**새로운 작업을 시작하기 전에 일단 pull 한다**고 생각하면 대용.

---

## 브랜치 생성

### main에서 바로 개발하지 마세용!!!!!

우리는 새로운 기능을 개발하거나 코드를 수정할 때마다  
**새로운 브랜치를 만들어서 작업할 거예용.**

```shell
# 새로운 브랜치를 만들면서 그 브랜치로 이동
git switch -c 브랜치이름
```

예를 들어

```shell
git switch -c feat/login
```

이렇게 하면

```text
main
 └── feat/login  ← 지금 여기서 작업
```

대충 이런 상태가 대용.

현재 내가 어느 브랜치에 있는지 모르겠다면

```shell
git branch
```

를 입력하세용.

예를 들어

```text
  main
* feature-login
```

처럼 나온다면 `*`가 붙어있는 `feature-login` 브랜치에서 작업하고 있다는 뜻이에용.

### 중요!!!

**코드 수정하기 전에 내가 무슨 브랜치에 있는지 확인하세용.**

```shell
git branch
```

`main`에 있다면 그냥 개발하지 말고 새로운 브랜치를 만드세용.

```shell
git switch -c 브랜치이름
```

---

## 코드 수정 / 개발

이제 Visual Studio Code에서 마음껏 코드를 수정하세용.

현재 폴더를 Visual Studio Code로 열고 싶다면

```shell
code .
```
코드를 수정하고 저장하세용.

뭘 수정했는지 궁금하다면

```shell
git status
```

---

## add

개발이 어느 정도 끝났으면 변경한 파일들을 staging area에 올리세용.

```shell
git add .
```

`git add .`은 현재 디렉토리 아래의 변경사항을 한 번에 추가해줘용.

잘 올라갔는지 확인하세용.

```shell
git status
```

---

## commit

이제 commit을 만드세용.

```shell
git commit -m "커밋 메시지"
```

예를 들어 로그인 기능을 만들었다면

```shell
git commit -m "로그인 기능 구현"
```

커밋 메시지는

```text
수정
ㅋㅋ
asdf
123
```

같이 쓰지 마세용. 
**내가 무엇을 했는지 알아볼 수 있게 작성하세용.**
그리고 모든 작업을 하나의 거대한 커밋으로 만들기보다는  
**의미 있는 작업 단위로 commit 하세용.**

---

## push

commit까지 만들었으면 GitHub에 올리세용.

```shell
git push -u origin 브랜치이름
```

예를 들어

```shell
git push -u origin feat/login
```

---

## Pull Request(PR) 만들기

push를 했다고 해서 아직 `main`에 코드가 들어간 것은 아니에용.

지금 상태는 대충

```text
main
 └── feature-login
       ├── commit
       ├── commit
       └── commit
```

이런 느낌이에용.

우리가 만든 `feat/login`의 변경사항을  
`main`에 합쳐달라고 요청해야 해용.

이 요청이 **Pull Request**, 줄여서 **PR**이에용.

### Github에서 PR 만들기

1. github.com에 들어가세용
    
2. 작업 중인 repository에 들어가세용
    
3. 위쪽의 `Pull requests`를 누르세용
    
4. `New pull request`를 누르세용
    
5. 아래처럼 되어있는지 확인하세용
    

```text
base: main  ←  compare: 내가만든브랜치
```

예를 들어

```text
base: main  ←  compare: feat/login
```

이어야 해용.
**방향 반대로 하지 마세용!!!!!**

6. `Create pull request`를 누르세용
    
7. 제목을 적으세용
    
8. 무엇을 수정했는지 설명을 적으세용
    
9. 다시 `Create pull request`를 누르세용
    

이제 PR이 만들어졌어용.

---

## PR 확인하기

PR을 만들었으면 바로 merge 버튼부터 누르지 마세용.

한 번 확인하고 가세용.

특히

```text
Files changed
```

를 눌러보세용.

내가 수정한 코드가 나와용.

여기서

- 이상한 파일을 같이 올리지는 않았는지
    
- 지우면 안 되는 코드를 지우지는 않았는지
    
- 내가 수정하려던 내용이 맞는지
    

확인하세용.

협업 프로젝트라면 다른 사람이 PR을 보고 리뷰를 해줄 수도 있어용.

---

## Merge

PR에 문제가 없다면 `main`에 합치세용.

Github PR 페이지에서

```text
Merge pull request
```

버튼을 누르고

```text
Confirm merge
```

를 누르세용.

그러면 내가 만든 브랜치의 변경사항이 `main`에 들어가용.

대충

```text
main ───────────────●
       \            /
        ●──●──●────
        feature-login
```

이런 일이 일어난 거예용.

### 중요!!!

협업 프로젝트에서는  
**리뷰를 받아야 하는데 혼자 마음대로 merge하면 안 될 수도 있어용.**
프로젝트마다 규칙이 다르니 팀의 규칙을 따르세용.

---

## Merge가 끝났다면

Github에서 merge했다고 해서  
**내 컴퓨터의 main 브랜치도 자동으로 바뀌는 것은 아니에용.**

Github의 `main`은 최신인데

내 컴퓨터의 `main`은 옛날 상태일 수 있어용.

그래서 다시 `main`으로 이동하세용.

```shell
git switch main
```

그리고 Github의 최신 내용을 가져오세용.

```shell
git pull
```

그러면 이제 내 컴퓨터의 `main`도 최신 상태가 대용.

---

## 진짜 최소 행동강령

헷갈리면 이것만 기억하세용.

**1. main에서 개발하지 마세용**

**2. 개발 시작 전에 main에서 `git pull` 하세용**

**3. 새로운 작업마다 새로운 branch를 만드세용**

**4. 개발했으면 `add → commit → push` 하세용**

**5. Github에서 PR을 만드세용**

**6. PR을 확인하고 merge하세용**

**7. 다시 main으로 돌아와 `git pull` 하세용**

이 흐름에 익숙해지는 것이 이번 실습의 목표예용.
